# Implementation Plans

**Implementing? Start at [`000-execution-runbook.md`](000-execution-runbook.md)**
— it owns sequencing, the per-plan protocol, and the STOP/escalation rules.

Generated by the improve skill on 2026-06-11 (audit of portfolio-2026 @ `b1fe917`
and denizlg24-app @ `e790966`; repo root was not yet a git repo). Revised same
day after stack alignment with the maintainer. Execute in the order below.
Each executor: read the plan fully before starting, honor its STOP conditions,
and update your row when done.

## Agreed stack decisions (2026-06-11, maintainer-confirmed)

These are settled — executors and future advisors do not re-litigate:

1. **Turborepo** (not plain bun workspaces) — for Vercel zero-config deploys
   and Remote Caching.
2. **Scaffold-first**: structure comes from `bunx create-turbo@latest` and
   `bunx create-next-app@latest`; code is ported INTO scaffolds. Never
   hand-author package.json manifests; dependencies via `bun add` only.
3. **Latest versions always**: `bun add` without pins; major-version breaks
   are reported, not silently pinned (STOP conditions in plan 003).
4. **Type safety via zod**: `packages/schemas` is the canonical API contract;
   TS types are `z.infer` only. Plain shared interfaces rejected.
5. **Naming**: `apps/web` (portfolio backend + public site), `apps/desktop`
   (Tauri client). Packages namespaced `@repo/*`.
6. **Scope**: web + desktop only. `bookmark-extension/`,
   `deniz-nutrition-api/`, `macros/` stay out (gitignored).
7. **CI**: GitHub Actions + Dependabot, with a workflow that auto-repairs
   `bun.lock` on Dependabot PRs.
8. Trade-off accepted: fresh root git history; originals preserved with full
   history under `_archive/`.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Verification baseline (typecheck + test scripts) | P1 | S | — | DONE (2026-06-11: zero pre-existing failures — see `baseline.md`; portfolio-2026 had 4 test files, not 2, all passing) |
| 002 | Harden public endpoints (rate limits, pagination, mass assignment) | P1 | M | 001 | DONE (2026-06-11: all 4 fixes landed; smoke test confirmed 429 after 5 contact POSTs — 5 test contacts left in local DB) |
| 003 | Turborepo conversion (scaffold-first, apps/web + apps/desktop) | P1 | L | 001, 002 | DONE (2026-06-11: all gates pass incl. FULL TURBO cache + tauri dev; major bumps lucide-react 0.556→1.17, react-day-picker 9→10, recharts 2.15→3.8 required documented migrations — see commit messages; phantom deps mongodb/katex/pdfjs-dist/@radix-ui/react-dialog declared explicitly) |
| 004 | Shared zod schema package (`@repo/schemas` + `@repo/utils`) | P1 | L | 003 | DONE (2026-06-11: all gates green; zero desktop call sites broken by shim; sourceRepository drift fix includes `url` field the plan excerpt omitted; package tsconfigs need `module: Preserve`/`moduleResolution: Bundler` + explicit `types: ["bun"]` under TS 6; blog-update-schema.test.ts import also relocated) |
| 005 | Spike: responsive admin design + shared UI direction | P2 | M | 003 (004 ideally) | IN PROGRESS |
| 006 | GitHub Actions CI + Dependabot + lockfile auto-repair | P2 | S | 003 | DONE (2026-06-12: 3 files under `.github/`; initially BLOCKED on pre-existing Biome failures (CRLF from `core.autocrlf=true` + real lint errors), operator authorized fixing — `.gitattributes` `eol=lf`, biome migrate 2.4.16 + `tailwindDirectives`, 181-file format pass, optional-chain/banned-type/a11y fixes; full chain green) |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (reason) | REJECTED (rationale)

## Dependency notes

- 002 before 003: security fixes land while file paths are stable; 003 ports
  the whole tree to `apps/` (and the fixes travel with it).
- 003 creates the root git repo and the `monorepo-cutover` tag that 004/005/006
  drift checks reference.
- 004 relocates plan 002's `blog-update-schema.ts` into `@repo/schemas`.
- 006 can run any time after 003; before 004/005 is fine and gives them CI
  coverage.
- 005's deliverable is a design doc that spawns follow-up plans (007+),
  including the future `packages/ui`.

## Audit findings not planned (backlog)

Real findings, deliberately not planned now — revisit after 005:

- **SEC-04** LLM prompt injection: untrusted email subject/from/body
  concatenated into triage prompts with tool use enabled
  (`lib/triage.ts:721-728`, pre-port path). Fix: delimit untrusted content +
  system-prompt hardening. M effort.
- **SEC-09** Static IMAP encryption key from env, no rotation/versioning
  (`lib/safe-email-password.ts`). Encryption itself is AES-256-GCM (fine). L effort.
- **BUG-04** Email sync cron returns 200 even when every account fails
  (`app/api/jobs/email/route.ts:23-33`) — monitoring blind spot. S/M effort.
- **BUG-03** Kanban drag optimistic update not snapshot-rolled-back on API
  failure (desktop `kanban-board.tsx:333-394`). M effort.
- **BUG-06** Window listeners can persist if kanban unmounts mid-drag
  (`kanban-board.tsx:66-105`). S effort.
- **PERF-03** Initial IMAP sync: per-email DB writes, no batching/limit
  (`lib/sync-email.ts:50-110`). M effort.
- **PERF-04** Desktop bundle: heavy deps (@fortune-sheet, force-graph, pdf)
  with only 2 `dynamic()` usages found — needs build analysis. M effort.
- **TEST-02/03** Zero tests on `require-admin`, contact flow, email sync —
  partially addressed by 001/002; full characterization suite remains. L effort.
- **DEBT-02** Note-group tree/hierarchy logic duplicated with divergent APIs
  (desktop `lib/note-group-tree.ts` vs web `lib/note-group-hierarchy.ts`) —
  consolidate into `@repo/utils` after 004. M effort.
- **DEBT-05** God files: `lib/triage.ts` (~1300 lines), `chat-view.tsx`
  (~1290), `person-detail.tsx` (~1240). Real but L effort / HIGH risk without
  tests — needs characterization tests first.
- **DEBT-07** Two shadcn `components/ui` copies, divergence unverified — must
  be diffed when `packages/ui` is created (handled inside 005's doc).
- **Backend schema sweep**: validate ALL route request bodies against
  `@repo/schemas` (004 does two exemplars). M/L effort, after 004.
- **SEC-05-adjacent** Other public endpoints returning zod issue details —
  002 fixes contact; sweep the rest during the backend schema sweep.

## Findings considered and rejected

So nobody re-audits these:

- **SEC-01 "secrets committed in .env"**: FALSE — `.env` is gitignored and not
  tracked (`git ls-files` shows only `.env.example`); auditor read the local
  file. Local plaintext `.env` is standard practice. The tracked
  `.envoy/config.toml` was checked: 11 lines, no secrets.
- **BUG-01 "rate-limit check-then-increment race"**: the `findOneAndUpdate`
  uses an atomic aggregation-pipeline update; the over-limit `$pop` removes
  the just-appended timestamp. Design is sound.
- **SEC-11 "API key brute force"**: keys stored as SHA-256 hashes, compared
  by hash lookup; search space makes online brute force a non-issue.
- **SEC-08 "missing CSRF"**: better-auth ships CSRF protections and modern
  SameSite cookie defaults; admin API additionally accepts Bearer keys (CSRF
  proof). Downgraded to "verify better-auth cookie config once".
- **SEC-07 IDOR on blogs**: single-admin system by design.
- **SEC-10 "stack traces leak"**: `console.error` goes to server logs, not
  responses. Misread.
- **SEC-13 blog slug uniqueness**: LOW confidence, functional not security.
- **SEC-03 IMAP password input validation**: library handles it; negligible upside.
- **jobs/* route auth**: checked — all five cron routes verify a Bearer token
  against env. Clean.
- **Plain shared-interface package** (first draft of plan 004): superseded by
  the zod-schema decision; draft deleted.
- **History-preserving `git subtree` import** (first draft of plan 003):
  superseded by scaffold-first decision; history lives in `_archive/` instead.
