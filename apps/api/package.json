{
  "name": "api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "auth:migrate-users": "bun scripts/migrate-users.ts",
    "dev": "bun run --watch src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@better-auth/drizzle-adapter": "^1.6.25",
    "@repo/cloud-core": "workspace:*",
    "@repo/schemas": "workspace:*",
    "better-auth": "^1.6.25",
    "drizzle-orm": "^0.45.2",
    "hono": "^4.12.31",
    "redis": "^6.1.0"
  },
  "devDependencies": {
    "@aws-sdk/client-s3": "^3.1094.0",
    "@repo/typescript-config": "workspace:*",
    "@types/bun": "^1.3.14",
    "tus-js-client": "^4.3.1",
    "typescript": "^5.9.3"
  }
}
