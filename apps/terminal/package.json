{
  "name": "terminal",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "build": "bun build src/index.ts --compile --outfile dist/cloud-terminal",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@repo/cloud-core": "workspace:*",
    "@repo/schemas": "workspace:*",
    "bun-pty": "^0.4.10"
  },
  "devDependencies": {
    "@repo/typescript-config": "workspace:*",
    "@types/bun": "^1.3.14",
    "typescript": "^5.9.3"
  }
}
