# Long-running DR units publish atomically and naturally start fresh on their
# next timer invocation. Do not terminate an in-flight transaction after a
# library upgrade; zero means "do not select for restart" in needrestart.
$nrconf{override_rc}{qr(^deniz-dr-(?:backup|r2-sync|r2-retention)@)} = 0;
